version: "3.9"

services:
  dash:
    build:
      context: .
      dockerfile: Dockerfile
    image: a3_anushka_final:latest
    ports:
      - "8060:80"
    volumes:
      - ./code:/root/code
      - ./_mlflow/mlruns:/mlflow/mlruns
    command: ["python3", "/root/code/app.py"]
    environment:
      # MLflow connection
      PYTHONPATH: "/root/code"
      MLFLOW_TRACKING_URI: "https://admin:password@mlflow.ml.brain.cs.ait.ac.th/"
      MODEL_URI: "runs:/affa6ea96a9a4b42938895d7753ae526/model"
    networks:
      - car-network

networks:
  car-network:
    driver: bridge